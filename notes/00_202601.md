20260105:

docker部署？
现有用增的前端项目也是docker?jenkins流程；

细节笔记：
1、export class AuthGuard implements CanActivate {} 
implements 的核心价值 ——强制类遵守接口规范，避免开发者漏写核心方法。
简单类比：
extends 是「父子继承」（比如「学生」继承「人」，能直接用「人」的方法）；
implements 是「签合同」（类承诺实现接口规定的所有方法，不实现就编译报错）。

调试学习：
调试技巧：视频+文档：
在控制器方法中添加debugger语句
使用VSCode的"调用堆栈"面板追踪请求流程
条件断点：右键断点设置表达式（如req.method === 'POST'）

什么是装饰器：
@Controller('api/v1/users') // 是一个装饰器（Decorator），用于将一个类标记为 NestJS 的控制器

postman模拟请求5种；

git filter-branch --env-filter '
OLD_EMAIL="guojc@g-bits.com"
CORRECT_NAME="flydoing"
CORRECT_EMAIL="1262868349@qq.com"
if [ "$GIT_COMMITTER_EMAIL" = "$OLD_EMAIL" ]
then
    export GIT_COMMITTER_NAME="$CORRECT_NAME"
    export GIT_COMMITTER_EMAIL="$CORRECT_EMAIL"
fi
if [ "$GIT_AUTHOR_EMAIL" = "$OLD_EMAIL" ]
then
    export GIT_AUTHOR_NAME="$CORRECT_NAME"
    export GIT_AUTHOR_EMAIL="$CORRECT_EMAIL"
fi
' --tag-name-filter cat -- --branches --tags
